# Bible Verse Hunt

Static web game. Deploy via Vercel/Netlify (see `vercel.json` / `netlify.toml`).

## Local

Open `index.html` in a browser, or serve with any static server.

## Health

`/health.json`
